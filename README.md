# Weby — monorepo (Vercel multi-domain)

Jeden Vercel projekt, více domén. Podrobnosti: **[DOMENY.md](./DOMENY.md)**.

```bash
npm run install:all
npm run build
```

Deploy: root `vercel.json`, výstup `dist/`.
