# Kopíruj jako .env.local v této složce (kadernictvi) — necommitovat.
# Tím získá agent přímý přístup k Postgresu (tabulky, RLS, migrace).

# Supabase → Project Settings → Database → Connection string → URI
# Session mode, pooler, port 6543 (nebo 5432 dle dashboardu)
DATABASE_URL=postgresql://postgres.hnkcjrvqbeojegujuuyw:TVÉ_HESLO_K_DATABÁZI@aws-0-eu-central-1.pooler.supabase.com:6543/postgres

# Volitelné — skripty přes REST (backfill tržeb), už máš v elegance .env.local:
# SUPABASE_URL=https://hnkcjrvqbeojegujuuyw.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=...
